import { RedColoredDirective } from './red-colored.directive';

describe('RedColoredDirective', () => {
  it('should create an instance', () => {
    const directive = new RedColoredDirective();
    expect(directive).toBeTruthy();
  });
});
